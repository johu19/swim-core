{
  "name": "finance-manager",
  "version": "0.1.0",
  "private": true,
  "description": "Lambda-first backend for a finance manager project.",
  "engines": {
    "node": "24.x"
  },
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "clean": "rm -rf dist",
    "create:table:transaction": "npm run build && node dist/scripts/create-transaction-table.js",
    "dev:invoke:health": "node dist/scripts/invoke-health.js",
    "start": "npm run build && npm run dev:invoke:health"
  },
  "dependencies": {
    "@aws-sdk/client-dynamodb": "^3.821.0"
  },
  "devDependencies": {
    "@types/aws-lambda": "^8.10.147",
    "@types/node": "^24.0.0",
    "typescript": "^5.8.2"
  }
}
