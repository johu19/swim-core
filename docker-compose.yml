services:
  dynamodb-local:
    image: amazon/dynamodb-local:latest
    container_name: swim-core-dynamodb-local
    command: '-jar DynamoDBLocal.jar -sharedDb -inMemory'
    ports:
      - '8000:8000'

  dynamodb-init:
    image: node:24-alpine
    working_dir: /workspace
    depends_on:
      - dynamodb-local
    env_file:
      - .env
    environment:
      DYNAMODB_ENDPOINT: http://dynamodb-local:8000
    volumes:
      - ./:/workspace
    command: npm run create:table:swim-core
